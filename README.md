# intro-to-github
This is a demo repository to practice using GitHub.

It has two files in the **Code** tab:
- **README.md** is a file that describes this repo (you are currently reading it)
- **.gitignore** is a file that specifies which files and directories must be ignored by Git

You cannot directly modify files in this repo because you are not a *collaborator*.

The **Issues** tab is used to discuss ideas, enhancements, bugs, questions, and so on. They are grouped by *Open* and *Closed*.

The **Pull requests** tab contains proposals to make some changes in the files located in the repository. Repo's owners may review a request and put your changes if they look good.

You can create an *Issue* or make a *Pull request (PR)* to contribute to the project.

If you want to propose some changes to this repo, you may *fork* it, modify the content, and create *PR*. A *fork* is just a copy that allows you to change the content without affection the original project.
这是一个用于练习使用 GitHub 的演示存储库。

它在 **Code** 选项卡中有两个文件：
- **README.md** 是一个描述这个 repo 的文件（你正在阅读它）
- **.gitignore** 是一个文件，指定 Git 必须忽略哪些文件和目录

您不能直接修改此 repo 中的文件，因为您不是*合作者*。

**问题**选项卡用于讨论想法、增强功能、错误、问题等。 它们按 *Open* 和 *Closed* 分组。

**拉取请求**选项卡包含对位于存储库中的文件进行一些更改的建议。 Repo 的所有者可能会审查请求并在它们看起来不错的情况下进行更改。

您可以创建 *Issue* 或发出 *Pull request (PR)* 来为项目做出贡献。

如果您想对该 repo 提出一些更改，您可以 *fork* 它，修改内容，然后创建 *PR*。 *fork* 只是一个副本，它允许您在不影响原始项目的情况下更改内容。
